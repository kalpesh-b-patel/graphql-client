import React from 'react';

const Navbar = () => (
  <div className="row justify-between items-center p-2 bg-gray-600 text-amber-200">
    <div className="flex">Brand</div>
    <div className="flex">Menu</div>
  </div>
);

export default Navbar;
